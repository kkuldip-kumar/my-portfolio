export type Project = {
    id: number,
    name: string,
    tag: string,
    description: string,
    imgags: string
    url: string
}

export type skill = {
    id: number,
    center: boolean,
    name: string,
    url: string
}
export type tag = {
    id: number,
    name: string,
}